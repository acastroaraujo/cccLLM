{
  "name": "summary_schema",
  "strict": true,
  "schema": {
    "type": "object",
    "required": [
      "summary",
      "chamber_raw",
      "magistrados",
      "articles",
      "rj",
      "rj_citation",
      "amicus"
    ],
    "properties": {
      "summary": {
        "description": "Resumen detallado de la sentencia. Debe contener los hechos del caso y la decisión tomada por la Corte.",
        "type": "object",
        "required": [
          "english",
          "spanish"
        ],
        "properties": {
          "english": {
            "type": "string",
            "description": "In English."
          },
          "spanish": {
            "type": "string",
            "description": "En Español."
          }
        },
        "additionalProperties": false
      },
      "chamber_raw": {
        "type": "string",
        "description": "Nombre de la sala, en caso de que la información sea explícita."
      },
      "magistrados": {
        "type": "array",
        "description": "Información sobre los magistrados que firmaron la sentencia. El secretario general está excluído de esta lista.",
        "items": {
          "type": "object",
          "required": [
            "name",
            "av",
            "sv",
            "mp",
            "interim",
            "conjuez"
          ],
          "properties": {
            "name": {
              "type": "string",
              "description": "Nombre."
            },
            "av": {
              "type": "boolean",
              "description": "Incluye aclaración de voto?"
            },
            "sv": {
              "type": "boolean",
              "description": "Incluye salvamento de voto?"
            },
            "mp": {
              "type": "boolean",
              "description": "Es el magistrado ponente?"
            },
            "interim": {
              "type": "boolean",
              "description": "Es magistrado encargado?"
            },
            "conjuez": {
              "type": "boolean",
              "description": "Es conjuez?"
            }
          },
          "additionalProperties": false
        }
      },
      "articles": {
        "type": "array",
        "description": "Lista de artículos de la Constitución que son objeto de discusión, en caso de que la información sea explícita. Excluir otro tipo de artículos.",
        "items": {
          "type": "integer"
        }
      },
      "rj": {
        "type": "string",
        "description": "Indica si la decisión se fundamenta en cosa juzgada, es decir, si ya ha sido resuelta previamente por la Corte con efectos vinculantes que impiden que el mismo asunto constitucional sea discutido nuevamente.",
        "enum": [
          "sí",
          "no",
          "parcial"
        ]
      },
      "rj_citation": {
        "type": "array",
        "description": "En caso de que la información sea explícita, lista de sentencias previas que llevaron a la Corte a decidir que el caso es \"cosa juzgada\". Las sentencias que no contribuyen a que el caso sea \"cosa juzgada\" están excluídas de esta lista.",
        "items": {
          "type": "string"
        }
      },
      "amicus": {
        "type": "array",
        "description": "Lista de intervinientes en calidad de \"amicus curiae\" en el proceso. Esta lista puede incluir personas naturales, ONG, asociaciones profesionales, sindicatos, universidades, entidades gubernamentales, y otros tipos de organizaciones públicas o privadas.",
        "items": {
          "type": "string"
        }
      }
    },
    "additionalProperties": false
  }
}
